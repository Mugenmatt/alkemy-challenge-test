import React from 'react';

// Utilizado para otorgar un feedback al usuario mientras se está procesando una petición. Para renderizar el loader, agregar la clase alkemy-spinner a un elemento.

const Loader = () => {
    return (
        <div class="alkemy-spinner"></div>
    );
};

export default Loader;